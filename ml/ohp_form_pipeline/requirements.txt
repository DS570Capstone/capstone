# Inference dependencies — sufficient to run run_single_video.py and the web API.
# For training, batch processing, or VLM/depth, also install requirements-train.txt.

# Core
numpy>=1.24
scipy>=1.10
pandas>=2.0
pyyaml>=6.0
tqdm>=4.65

# Computer Vision
opencv-python>=4.8
mediapipe>=0.10

# Signal processing / ML
scikit-learn>=1.3

# Visualization (lazy-loaded — only imported when save_plots=true)
matplotlib>=3.7
Pillow>=10.0

# Utilities
python-dotenv>=1.0
